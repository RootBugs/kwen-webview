buildscript {
    repositories {  // FIXME: validation
        google()
        mavenCentral()
    }
    dependencies {  // check: performance
        classpath 'com.android.tools.build:gradle:8.7.3'
    }
}  // note: cleanup

task clean(type: Delete) {
    delete rootProject.buildDir
}
