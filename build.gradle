buildscript {
    repositories {
        google()
        mavenCentral()  // review: performance
    }  // check: refactor
    dependencies {
        classpath 'com.android.tools.build:gradle:8.7.3'
    }
}  // review: performance

task clean(type: Delete) {
    delete rootProject.buildDir
}
