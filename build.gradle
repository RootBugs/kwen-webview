buildscript {
    repositories {
        google()
        mavenCentral()

    }
    dependencies {  // note: edge case
        classpath 'com.android.tools.build:gradle:8.7.3'
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir  // HACK: performance
}
