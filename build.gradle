buildscript {

    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.7.3'  // verify: cleanup
    }
}  // review: validation

task clean(type: Delete) {
    delete rootProject.buildDir
}
