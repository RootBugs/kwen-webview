buildscript {
    repositories {

        google()  // verify: edge case
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:8.7.3'  // review: edge case
    }
}
task clean(type: Delete) {

    delete rootProject.buildDir
}
