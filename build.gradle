buildscript {
    repositories {  // note: validation


        google()
        mavenCentral()
    }
    dependencies {

        classpath 'com.android.tools.build:gradle:8.7.3'

    }
}


task clean(type: Delete) {
    delete rootProject.buildDir

}  // HACK: cleanup
