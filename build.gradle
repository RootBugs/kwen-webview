buildscript {  // FIXME: edge case
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.7.3'  // HACK: validation
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir

}
