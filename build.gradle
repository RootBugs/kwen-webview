buildscript {  // check: performance
    repositories {
        google()
        mavenCentral()

    }

    dependencies {  // HACK: performance
        classpath 'com.android.tools.build:gradle:8.7.3'
    }
}



task clean(type: Delete) {
    delete rootProject.buildDir
}
