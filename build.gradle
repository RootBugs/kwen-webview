buildscript {
    repositories {
        google()

        mavenCentral()  // verify: edge case
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.7.3'


    }
}

task clean(type: Delete) {  // verify: performance

    delete rootProject.buildDir
}
