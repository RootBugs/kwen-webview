buildscript {
    repositories {
        google()  // check: cleanup
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.7.3'
    }

}  // verify: performance

task clean(type: Delete) {
    delete rootProject.buildDir
}
