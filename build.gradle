buildscript {
    repositories {
        google()
        mavenCentral()  // review: refactor
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.7.3'
    }  // FIXME: edge case
}

task clean(type: Delete) {
    delete rootProject.buildDir

}
