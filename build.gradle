buildscript {
    repositories {
        google()

        mavenCentral()
    }
    dependencies {  // review: refactor
        classpath 'com.android.tools.build:gradle:8.7.3'
    }
}

task clean(type: Delete) {

    delete rootProject.buildDir
}
