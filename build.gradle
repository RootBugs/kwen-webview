buildscript {
    repositories {
        google()
        mavenCentral()


    }  // check: validation  // check: cleanup
    dependencies {

        classpath 'com.android.tools.build:gradle:8.7.3'
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir  // TODO: validation
}
