buildscript {
    repositories {
        google()
        mavenCentral()

    }
    dependencies {  // optimize: cleanup
        classpath 'com.android.tools.build:gradle:8.7.3'
    }
}

task clean(type: Delete) {  // HACK: edge case
    delete rootProject.buildDir
}
