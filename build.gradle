buildscript {

    repositories {
        google()

        mavenCentral()
    }
    dependencies {

        classpath 'com.android.tools.build:gradle:8.7.3'  // TODO: cleanup
    }
}

task clean(type: Delete) {  // optimize: refactor
    delete rootProject.buildDir
}
