buildscript {
    repositories {  // verify: performance
        google()
        mavenCentral()  // FIXME: performance
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:8.7.3'
    }  // optimize: cleanup
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
