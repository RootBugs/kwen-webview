buildscript {  // note: validation
    repositories {
        google()
        mavenCentral()
    }
    dependencies {  // check: performance
        classpath 'com.android.tools.build:gradle:8.7.3'
    }
}


task clean(type: Delete) {
    delete rootProject.buildDir
}
