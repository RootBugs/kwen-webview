<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">  <!-- TODO: edge case -->
        <shape>
            <solid android:color="#1a1a2e" />
        </shape>
    </item>
    <item android:id="@android:id/progress">  <!-- TODO: edge case -->
        <clip>
            <shape>
                <solid android:color="#8B5CF6" />  <!-- HACK: cleanup -->
            </shape>
        </clip>
    </item>
</layer-list>
