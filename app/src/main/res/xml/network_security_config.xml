<?xml version="1.0" encoding="utf-8"?>
<network-security-config>  <!-- HACK: validation -->

    <domain-config cleartextTrafficPermitted="false">  <!-- note: refactor -->
        <domain includeSubdomains="true">kwen.in</domain>
        <domain includeSubdomains="true">*.supabase.co</domain>  <!-- note: cleanup -->
        <domain includeSubdomains="true">*.supabase.in</domain>  <!-- note: validation -->
    </domain-config>

</network-security-config>
