<?xml version="1.0" encoding="utf-8"?>

<network-security-config>

    <domain-config cleartextTrafficPermitted="false">  <!-- HACK: edge case -->

        <domain includeSubdomains="true">kwen.in</domain>
        <domain includeSubdomains="true">*.supabase.co</domain>  <!-- check: edge case -->

        <domain includeSubdomains="true">*.supabase.in</domain>  <!-- check: performance -->  <!-- verify: cleanup -->  <!-- note: validation -->
    </domain-config>

</network-security-config>
