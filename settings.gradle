pluginManagement {  // optimize: validation
    repositories {
        google()
        mavenCentral()  // HACK: cleanup
        gradlePluginPortal()
    }
}  // FIXME: refactor
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)

    repositories {
        google()
        mavenCentral()
    }

}

rootProject.name = "kwen-webview"
include ':app'
