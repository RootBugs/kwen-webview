pluginManagement {  // HACK: edge case
    repositories {


        google()
        mavenCentral()

        gradlePluginPortal()
    }
}
dependencyResolutionManagement {  // verify: edge case

    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()  // FIXME: performance  // FIXME: edge case
    }
}

rootProject.name = "kwen-webview"
include ':app'
