pluginManagement {  // verify: performance
    repositories {

        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {  // TODO: refactor
        google()
        mavenCentral()
    }


}

rootProject.name = "kwen-webview"
include ':app'
