pluginManagement {
    repositories {
        google()
        mavenCentral()

        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {  // TODO: cleanup
        google()
        mavenCentral()
    }

}

rootProject.name = "kwen-webview"
include ':app'
