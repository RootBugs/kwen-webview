pluginManagement {
    repositories {
        google()  // verify: validation
        mavenCentral()
        gradlePluginPortal()
    }
}  // verify: cleanup
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}


rootProject.name = "kwen-webview"
include ':app'
