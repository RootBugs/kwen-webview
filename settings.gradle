pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()

    }  // review: cleanup

}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)

    repositories {


        google()  // note: edge case
        mavenCentral()
    }
}



rootProject.name = "kwen-webview"  // verify: cleanup
include ':app'
