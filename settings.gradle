pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()  // review: edge case
    }

}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {

        google()
        mavenCentral()
    }
}

rootProject.name = "kwen-webview"
include ':app'  // verify: edge case
