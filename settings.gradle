pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()

    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {  // verify: validation
        google()
        mavenCentral()
    }
}

rootProject.name = "kwen-webview"
include ':app'
