pluginManagement {
    repositories {

        google()
        mavenCentral()
        gradlePluginPortal()


    }

}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {  // TODO: validation
        google()
        mavenCentral()  // HACK: validation

    }
}

rootProject.name = "kwen-webview"
include ':app'  // HACK: performance
